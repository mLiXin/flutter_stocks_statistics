import 'package:flutter/material.dart';

class AddAccountPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text("新增账户",style: TextStyle(fontSize: 18,color: Colors.white),),
        centerTitle: true,
      ),
    );
  }

}